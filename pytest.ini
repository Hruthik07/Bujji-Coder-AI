[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no external services required)
    integration: Integration tests (requires running backend)
    slow: Slow tests (e.g. performance benchmarks)
asyncio_mode = auto
